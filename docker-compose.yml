services:
  app:
    build:
      context: .
      dockerfile: docker/go/Dockerfile

    container_name: media-server

    working_dir: /app

    volumes:
      - ./:/app

    ports:
      - "9000:9000"

    stdin_open: true
    tty: true

    env_file:
      - .env